# providers

Swappable STT/TTS provider adapters (`stt.py`, `tts.py`) behind small
interfaces, so engines can be exchanged (e.g. Chatterbox vs F5-TTS) without
touching the agent. Populated by issues #7 and #8.
